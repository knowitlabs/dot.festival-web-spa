import { Container } from '../../components/layout/Container';
import { Section } from '../../components/layout/Section';

const html = String.raw;

export const ProgramPage = () => {
  return html` ${Section(Container('<h1 class="dot">Cooming soon!</h1>'))}`;
};
