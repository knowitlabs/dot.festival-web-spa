.dot.section
  & .dot.container
    p.dot.about
      color var(--dot-color-white)
      font-family var(--dot-font-family-default)
      font-size 26px
      font-style normal
      font-weight 400
      line-height 130%
      margin-bottom 4rem

    h2.dot.about
        color var(--dot-color-white)