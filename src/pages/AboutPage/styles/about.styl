.dot.section
  & .dot.container
    p.dot
      color var(--dot-color-white) !important
      font-family var(--dot-font-family-default)
      font-size 26px
      font-style normal
      font-weight 400
      line-height 130%
      margin-bottom 4rem

    h2.dot
        color var(--dot-color-white) !important